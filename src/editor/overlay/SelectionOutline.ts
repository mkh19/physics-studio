import { DisplayObject } from "../../graphics";
import { CanvasRenderer } from "../../renderer";

export class SelectionOutline {

    public draw(
        renderer: CanvasRenderer,
        object: DisplayObject
    ): void {

        // سننفذ الرسم في الخطوة التالية.

    }

}